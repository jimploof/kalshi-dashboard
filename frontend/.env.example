# Frontend non-secret configuration only.
# Copy to frontend/.env: cp frontend/.env.example frontend/.env
# Do NOT place backend URLs, API keys, or secrets here.

# Example placeholder — add browser-safe flags as needed.
# NG_APP_TITLE=Kalshi Dashboard
